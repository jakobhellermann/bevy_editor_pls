[package]
name = "basic"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_editor_pls = { path = "../../crates/bevy_editor_pls" }
bevy = { version = "0.6", default-features = false, features = [
    "bevy_winit",
    "x11",
] }
